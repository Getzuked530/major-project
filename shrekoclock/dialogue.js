let dialogueOptions = [];

function dialogue(){
    dialogueOptions.push( new StoryText("Ok Boomer", windowWidth/2, windowHeight/2));
    dialogueOptions.push( new StoryText("Ok Boomer!!!", windowWidth/2, windowHeight/2));
}